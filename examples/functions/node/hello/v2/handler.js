exports.handler = async (event) => { const q = event.query || {}; const env = event.env || {}; const ctx = event.context || {}; const name = q.name || "world"; const debugEnabled = !!(ctx.debug && ctx.debug.enabled === true); const payload = { hello: (env.NODE_GREETING || "v2") + "-" + name }; if (debugEnabled) { payload.debug = { request_id: event.id, runtime: "node", function: "hello", trace_id: (ctx.user || {}).trace_id }; } return { status: 200, headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) }; };
